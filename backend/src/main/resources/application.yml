spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cloudyprotection
    username: cloudy
    password: cloudy
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  jackson:
    serialization:
      indent_output: true

server:
  port: 8080

app:
  jwt:
    secret: change-me-in-prod
    expiration: 3600